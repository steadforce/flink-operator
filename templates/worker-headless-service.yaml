apiVersion: v1
kind: Service
metadata:
  name: {{ template "flink.name" . }}-headless-worker
  namespace: {{ .Release.Namespace }}
spec:
  clusterIP: None
  ports:
    - name: tcp-flink
      port: {{ .Values.flink.service.port }}
      protocol: TCP
  selector:
    app: {{ template "flink.name" . }}
    component: worker
  type: ClusterIP